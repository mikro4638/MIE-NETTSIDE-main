// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  base: '/MIE-NETTSIDE-main/',   // 👈 viktig for GitHub Pages
  outDir: './dist',              // standard build-mappe
});
